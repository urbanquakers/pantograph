source('https://rubygems.org')

gem 'xcode-install'

gem 'danger'
gem 'danger-junit'

gemspec(path: '.')

# plugins_path = File.join(File.expand_path('..', __FILE__), 'pantograph', 'Pluginfile')
# eval_gemfile(plugins_path)
